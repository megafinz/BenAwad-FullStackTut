POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Defaults to redis://localhost:6379 if omitted,
# replace host with "redis" if running through docker-compose
REDIS_URL=redis://redis:6379
REDIS_SECRET=%GENERATE_SOME_SECRET_VALUE_HERE%

# Defaults to http://localhost:3000 if omitted
WEB_APP_BASE_URL=http://localhost:3000

# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Replace host with "db" if running through docker-compose
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fullstacktut?schema=public"
